# Import Omarchy default settings
include=~/.local/share/omarchy/default/mako/core.ini

# Import current theme colors (auto-updates when theme changes)
include=~/.config/omarchy/current/theme/mako.ini

# Override with rounded corners to match Hyprland (12px)
border-radius=12

# Proper opacity (no weird transparency box)
background-color=#2c2525ee

