# Extra autostart processes
# exec-once = uwsm-app -- my-service

# NOTE: Omarchy starts waybar automatically via uwsm-app
# No need to start waybar here, it would create duplicates
# exec-once = waybar

# EasyEffects - Audio effects processor
# Runs in service mode (background) and persists through restarts
exec-once = uwsm-app -- easyeffects --gapplication-service
